name: "Deploy on Server"

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: "Deploy"
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v2.3.2